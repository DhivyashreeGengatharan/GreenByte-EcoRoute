# Server Configuration
PORT=5000

# Authentication
JWT_SECRET=your_jwt_secret_here

# Database (MongoDB Atlas or local MongoDB)
MONGODB_URI=mongodb://localhost:27017/greenbyte

# Mapbox (Optional - building density fallback used if omitted)
MAPBOX_ACCESS_TOKEN=
